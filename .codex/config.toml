# Project-scoped Codex defaults for Nimbus.
# These override user config only while you are inside this repo.

personality = "pragmatic"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

[sandbox_workspace_write]
writable_roots = []
network_access = false

[features]
codex_hooks = true
