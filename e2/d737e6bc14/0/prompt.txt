commit and push

---

Run REVIEW_ID=$(cat "$GITHUB_WORKSPACE/nimbus-review-id.txt" 2>/dev/null || echo "")
  REVIEW_ID=$(cat "$GITHUB_WORKSPACE/nimbus-review-id.txt" 2>/dev/null || echo "")
  
  if [ -z "${REVIEW_ID}" ]; then
    echo "::error::Review ID file was not produced by review create."
    exit 1
  fi
  
  case "${REVIEW_ID}" in
    rev_[a-z0-9]*) ;;
    *)
      echo "::error::Invalid review ID format: ${REVIEW_ID}"
      exit 1
      ;;
  esac
  
  echo "review_id=${REVIEW_ID}" >> "$GITHUB_OUTPUT"
  echo "Parsed review_id=${REVIEW_ID}"
  shell: /usr/bin/bash -e {0}
  env:
    ENTIRE_CHECKPOINTS_FETCHED: true
    PNPM_HOME: /home/runner/setup-pnpm/node_modules/.bin
Error: Invalid review ID format: review_9ypod5iy
Error: Process completed with exit code 1.