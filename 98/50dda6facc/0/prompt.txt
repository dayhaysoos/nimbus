commit the current unstaged changes for me. I'll go through the UI experience myself

---

so I'm looking at the UI right now, it never updated to reflect that it canwork now. I suppose I can refresh the page, but I'm dissapointed it didn't update immediately after you made the commit. is it possible to address this?

---

horrible experience so far. It said it was creating a review session, then refreshed the page..then left me staring at the mainpage to create a new review or resume the session. I clicked resume and got this: Session

Review session unavailable
Unexpected token 'N', "Not Found" is not valid JSON

Back to launch

---

I think this experience should only be focused on what's current. For exameple, if I hit "review" it should never allow me to create another review from the same commit. Once a review is kicked off and active, we should be able to watch it progress over time. Only once the review is fully resolved and a merge happens from the worktree, only then we should be able to see the "New review session" button. Maybe I'm not communicating what I'm looking for correctly, but do you understand what I mean about focusing on what's "current"?

---

can you build it right now or did you already start?

---

This is the result, it's really bad still. Why can't I adopt? It didn't make any changes? Review session

session_jy6nzlry
completed
Nimbus stopped because further review passes looked low-yield relative to the remaining issues.

Back to launch
Refresh canonical route
Repository
dayhaysoos/nimbus
Branch
feat/review-experience-ui
Mode
Intent-aware
Passes
1
Pass timeline

Review loop

Pass 1

succeeded
Diff wires Studio and local-environment flows to sessions, adds richer studio preflight, and factors local env helpers; overall risk medium; further review of ui-proxy.ts and truncated sections warranted.

Hide
Review ID
review_mpmbfdek
Basis
checkpoint
Started
4/15/2026, 8:55:54 AM
Pass findings
low
`resolveLocalReviewEnvironmentEntry` now optionally filters by `branchName` and `mode`, but `recordLocalReviewEnvironment` still keys entries only by `repoRoot`, `sessionId`, and `mode`. This allows multiple registry entries for the same session/branch/mode combination, and the new filtering logic can return an older or unexpected entry if duplicates exist.
Nimbus has not streamed live events for this pass in this browser session yet.
Outcome

Session result
Outcome
exhausted
Nimbus stopped because further review passes looked low-yield relative to the remaining issues.

Recommendation
comment
1 unresolved finding(s) remain.

Changed files
0
Nimbus did not publish a remediation summary.

Findings

Still open
low
packages/cli/src/app/reviews/local-environments.ts:188
`resolveLocalReviewEnvironmentEntry` now optionally filters by `branchName` and `mode`, but `recordLocalReviewEnvironment` still keys entries only by `repoRoot`, `sessionId`, and `mode`. This allows multiple registry entries for the same session/branch/mode combination, and the new filtering logic can return an older or unexpected entry if duplicates exist.
`resolveLocalReviewEnvironmentEntry` now optionally filters by `branchName` and `mode`, but `recordLocalReviewEnvironment` still keys entries only by `repoRoot`, `sessionId`, and `mode`. This allows multiple registry entries for the same session/branch/mode combination, and the new filtering logic can return an older or unexpected entry if duplicates exist.

Suggested fix
Either (a) update the registry key used in `entryKey` and `recordLocalReviewEnvironment` to include `branchName` so only one entry per (repoRoot, sessionId, mode, branchName) exists; or (b) when multiple entries match the same `sessionId`/`branchName`/`mode` in `resolveLocalReviewEnvironmentEntry`, consistently pick the most recent one (e.g., by `materializedAt`) instead of the first array element. Also consider de-duplicating old entries when writing the registry.

Findings

Resolved during this session
Nimbus did not resolve any previously emitted findings in this session.
Reviewed diff

What Nimbus changed
Session did not produce a remediated worktree diff.
Adopt

Bring the reviewed result local
Adopting locally creates an isolated worktree for this session so you can run the code and test it yourself before merging anything back.

Adopt locally

---

I really need this to make edits on the cloud so we can adopt. It came back with findings but didn't make changes? can you remind me what determines if changes get made or not?

---

I want to demo this to someone but I need to see the full UI through with changes that can be adopted. Do you think the current unstaged code will suffice?