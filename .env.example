# OpenRouter API Key (set as wrangler secret for worker)
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxx

# Worker URL (for CLI, defaults to localhost:8787 for local dev)
NIMBUS_WORKER_URL=http://localhost:8787
